import { ComingSoon } from "@/components/app/ComingSoon";

export const dynamic = "force-dynamic";

export default function SourcesPage() {
  return (
    <ComingSoon
      eyebrow="Sources"
      title="Here's how I learn you."
      body="Connections, questionnaires, and uploads (with gated sensitive sources) land in a later phase."
    />
  );
}
