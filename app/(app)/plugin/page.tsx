import { ComingSoon } from "@/components/app/ComingSoon";

export const dynamic = "force-dynamic";

export default function PluginPage() {
  return (
    <ComingSoon
      cool
      eyebrow="Plug in"
      title="Your tools don't just read you. They teach you back."
      body="Plug your clone into Claude, Cursor, and anything that speaks MCP. The two-way connector and flywheel land in the next phase."
    />
  );
}
